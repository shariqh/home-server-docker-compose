# 1Password secret references for the media stack.
# Safe to commit — these are pointers, not secret values. Resolved at
# `docker compose up` time by `op run`.

PIA_USERNAME=op://dev-env-vars/ubi-prod-envs/pia_username
PIA_PASSWORD=op://dev-env-vars/ubi-prod-envs/pia_password
