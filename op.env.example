# 1Password service-account token for op run bootstrap (up-all.sh,
# containerupdater.sh). Chicken-and-egg: this one value cannot live behind an
# op:// reference. Real file: op.env (chmod 600, git-ignored).
OP_SERVICE_ACCOUNT_TOKEN=ops_...
