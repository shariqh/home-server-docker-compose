# 1Password secret references for the infra stack.
# Safe to commit — these are pointers, not secret values. Resolved at
# `docker compose up` time by `op run`.

BESZEL_AGENT_KEY=op://home-server/ubi-prod-envs/beszel_agent_key
